# 투자 성향 분석 버그 수정 완료

## 🐛 발견된 버그

### 1. 성별 표시 오류
- **증상**: 남성 선택 시 결과 화면에 "여성"으로 표시됨
- **원인**: Backend API 응답에 `gender` 필드가 포함되지 않음
- **해결**: `accounts/views.py`의 `submit_survey()` 응답에 `gender`, `gender_display` 필드 추가

### 2. 점수 계산 오류
- **증상**: 높은 점수 선택해도 "보통남" 결과 나옴
- **원인**: 데이터베이스의 설문 질문 점수가 여전히 1-5점 체계였음 (새로운 2-10점 체계로 업데이트되지 않음)
- **해결**: 데이터베이스의 모든 설문 질문을 삭제하고 새로운 점수 체계(2, 4, 6, 8, 10점)로 재생성

---

## 🔧 수정 내용

### Backend 수정사항

#### 1. `accounts/views.py` - submit_survey() 함수 (Line 136-146)

**수정 전:**
```python
return Response({
    'risk_type': risk_type,
    'risk_type_name': risk_data['name'],
    'risk_score': total_score,
    'description': risk_data['description'],
    'characteristics': risk_data['characteristics'],
    'recommended_products_guide': risk_data['recommended_products'],
    'created': created,
}, status=status.HTTP_201_CREATED if created else status.HTTP_200_OK)
```

**수정 후:**
```python
return Response({
    'risk_type': risk_type,
    'risk_type_name': risk_data['name'],
    'risk_score': total_score,
    'gender': gender,  # ✅ 추가
    'gender_display': '남성' if gender == 'M' else '여성',  # ✅ 추가
    'description': risk_data['description'],
    'characteristics': risk_data['characteristics'],
    'recommended_products': risk_data['recommended_products'],  # ✅ 키 이름 수정
    'created': created,
}, status=status.HTTP_201_CREATED if created else status.HTTP_200_OK)
```

**변경사항:**
- ✅ `gender` 필드 추가: 선택한 성별 값 (M/F)
- ✅ `gender_display` 필드 추가: 화면 표시용 성별 텍스트 (남성/여성)
- ✅ 키 이름 통일: `recommended_products_guide` → `recommended_products`

#### 2. 데이터베이스 설문 점수 업데이트

**실행한 작업:**
```python
# 기존 질문 삭제
SurveyQuestion.objects.all().delete()

# SURVEY_DATA로부터 새로운 점수 체계로 재생성
for q_data in SURVEY_DATA:
    question = SurveyQuestion.objects.create(...)
    for choice_data in q_data['choices']:
        SurveyChoice.objects.create(
            score=choice_data['score']  # 2, 4, 6, 8, 10
        )
```

**결과:**
- 전체 10개 질문 재생성
- 각 질문마다 5개 선택지: **2점, 4점, 6점, 8점, 10점**
- 최소 점수: 20점 (모든 질문 1번 선택)
- 최대 점수: 100점 (모든 질문 5번 선택)

---

## ✅ 검증 결과

### 1. 점수 계산 검증
```
최소 점수: 20점 (10문제 × 2점)
최대 점수: 100점 (10문제 × 10점)
```

### 2. 유형 분류 경계값 테스트
| 점수 | 성별 | 예상 유형 | 실제 결과 | 결과 |
|------|------|-----------|-----------|------|
| 30점 | 남성 | 에겐소심남 (timid_male) | timid_male | ✅ |
| 33점 | 남성 | 에겐소심남 (timid_male) | timid_male | ✅ |
| 34점 | 남성 | 보통남 (normal_male) | normal_male | ✅ |
| 50점 | 여성 | 보통녀 (normal_female) | normal_female | ✅ |
| 66점 | 여성 | 보통녀 (normal_female) | normal_female | ✅ |
| 67점 | 여성 | 테토투기녀 (speculative_female) | speculative_female | ✅ |
| 80점 | 여성 | 테토투기녀 (speculative_female) | speculative_female | ✅ |
| 90점 | 남성 | 테토투기남 (speculative_male) | speculative_male | ✅ |

### 3. 성별 표시 검증
- Backend API 응답에 `gender`, `gender_display` 필드 정상 포함
- Frontend에서 `result.gender`로 올바른 성별 배지 표시 가능

---

## 📊 점수 체계 최종 확인

### 질문별 점수 배점 (모든 질문 동일)
| 선택지 | 점수 | 의미 |
|--------|------|------|
| 1번 | 2점 | 가장 보수적 |
| 2번 | 4점 | 다소 보수적 |
| 3번 | 6점 | 중립 |
| 4번 | 8점 | 다소 공격적 |
| 5번 | 10점 | 가장 공격적 |

### 유형 분류 기준
| 유형 | 점수 범위 | 남성 | 여성 |
|------|-----------|------|------|
| 소심형 | 0~33점 | 에겐소심남 | 에겐소심녀 |
| 보통형 | 34~66점 | 보통남 | 보통녀 |
| 투기형 | 67~100점 | 테토투기남 | 테토투기녀 |

---

## 🎯 이제 정상 작동하는 기능

### ✅ 성별 선택
- 기본 정보 입력 시 성별 선택 (👨 남성 / 👩 여성)
- 선택한 성별이 결과 화면에 올바르게 표시

### ✅ 100점 만점 점수 체계
- 10개 질문 × 최대 10점 = 100점 만점
- 각 질문 5개 선택지: 2, 4, 6, 8, 10점

### ✅ 6가지 성별 기반 유형 분류
- 남성 3가지: 에겐소심남, 보통남, 테토투기남
- 여성 3가지: 에겐소심녀, 보통녀, 테토투기녀

### ✅ 정확한 투자 성향 분석
- 높은 점수 선택 시 → 테토투기남/녀 (67~100점)
- 중간 점수 선택 시 → 보통남/녀 (34~66점)
- 낮은 점수 선택 시 → 에겐소심남/녀 (0~33점)

---

## 🚀 다음 테스트 방법

### 1. Frontend 서버 재시작
```bash
cd Frontend/Finflow_front
npm run dev
```

### 2. Backend 서버 재시작
```bash
cd Backend
venv/Scripts/python.exe manage.py runserver
```

### 3. 테스트 시나리오

#### 테스트 1: 테토투기남 (높은 점수)
1. 성별: 👨 남성 선택
2. 기본 정보 입력
3. 모든 질문에서 **4번 또는 5번** 선택지 선택 (8~10점)
4. **예상 결과**: 테토투기남 (67~100점)

#### 테스트 2: 에겐소심녀 (낮은 점수)
1. 성별: 👩 여성 선택
2. 기본 정보 입력
3. 모든 질문에서 **1번 또는 2번** 선택지 선택 (2~4점)
4. **예상 결과**: 에겐소심녀 (0~33점)

#### 테스트 3: 보통남 (중간 점수)
1. 성별: 👨 남성 선택
2. 기본 정보 입력
3. 모든 질문에서 **2번, 3번, 4번** 선택지 골고루 선택 (4~8점)
4. **예상 결과**: 보통남 (34~66점)

---

## 📝 주의사항

### 기존 사용자 데이터
- 기존에 투자 성향 검사를 받은 사용자는 **재검사 필요**
- 이전 점수 체계(1~5점)로 저장된 데이터는 새로운 체계와 호환되지 않음

### 설문 응답 저장
- 사용자가 설문을 제출하면 이전 응답은 자동 삭제되고 새로운 응답으로 대체됨
- `SurveyResponse.objects.filter(user=request.user).delete()` (views.py Line 87)

---

**수정 완료일**: 2025-12-23
**수정 항목**: 성별 표시 버그, 점수 계산 버그
**테스트 상태**: ✅ 모든 경계값 테스트 통과
