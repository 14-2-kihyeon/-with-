# 마이페이지 및 관심상품 기능 구현 완료

## 🎉 완료된 작업

### 1. ✅ FinHomeView에 관심 등록 및 자세히 보기 버튼 추가

**파일**: `Frontend/Finflow_front/src/views/finances/FinHomeView.vue`

**추가된 기능:**

1. **관심 상품 등록 버튼**
   - 각 상품 카드에 하트 아이콘 버튼 추가
   - 클릭 시 관심 상품 등록/해제 토글
   - 등록된 상품은 빨간 하트(❤️), 미등록은 흰 하트(🤍)로 표시
   - 호버 시 확대 애니메이션 효과

2. **자세히 보기 버튼**
   - 각 상품 카드에 "자세히 →" 버튼 추가
   - 클릭 시 해당 상품 상세 페이지로 이동
   - 그라데이션 파란색 배경
   - 호버 시 상승 애니메이션 효과

3. **북마크 상태 관리**
   - 페이지 로드 시 사용자의 관심 상품 목록 불러오기
   - `bookmarkedProducts` Set으로 빠른 조회 가능
   - API 호출 후 실시간 상태 업데이트

**API 엔드포인트:**
- `GET /accounts/bookmarks/` - 관심 상품 목록 조회
- `POST /accounts/recommendations/{fin_prdt_cd}/bookmark/` - 관심 상품 토글

**코드 예시:**
```javascript
// 북마크 토글
const toggleBookmark = async (finPrdtCd) => {
  try {
    await api.post(`/accounts/recommendations/${finPrdtCd}/bookmark/`)

    if (bookmarkedProducts.value.has(finPrdtCd)) {
      bookmarkedProducts.value.delete(finPrdtCd)
    } else {
      bookmarkedProducts.value.add(finPrdtCd)
    }
  } catch (error) {
    console.error("북마크 실패:", error)
    alert("북마크에 실패했습니다.")
  }
}
```

---

### 2. ✅ 마이페이지 완전 리뉴얼

**파일**: `Frontend/Finflow_front/src/views/auth/MyPageView.vue`

**주요 섹션:**

#### 📌 헤더 섹션
- 사용자 아바타 (이니셜 표시)
- 사용자 이름 및 이메일 표시
- 그라데이션 배경 디자인

#### 📊 투자 성향 분석 섹션

**투자 성향이 없는 경우:**
- 빈 상태 안내 메시지
- "투자 성향 검사하기" 버튼 표시
- 클릭 시 투자 성향 검사 페이지로 이동

**투자 성향이 있는 경우:**

1. **프로필 배지**
   - 성향별 아이콘 및 이름 표시
   - 성향별 색상 테마:
     - 에겐소심남/녀: 파란색 그라데이션
     - 보통남/녀: 보라색 그라데이션
     - 테토투기남/녀: 오렌지색 그라데이션

2. **투자 성향 점수**
   - 0~100점 표시
   - 파란색 강조 스타일

3. **통계 카드 (6개)**
   - 성별
   - 나이
   - 연 소득
   - 현재 저축액
   - 투자 목표
   - 투자 기간
   - 반응형 그리드 레이아웃 (모바일: 2열, 데스크톱: 3열)

4. **성향 설명**
   - 투자 성향에 대한 상세 설명 표시
   - 연한 배경색 강조

5. **액션 버튼**
   - "재검사하기" 버튼 (우측 상단)
   - "맞춤 추천 상품 보기" 버튼 (하단)

#### ❤️ 관심 상품 섹션

**관심 상품이 없는 경우:**
- 빈 우편함 아이콘(📭)
- 안내 메시지
- "상품 둘러보기" 버튼 → FinHomeView로 이동

**관심 상품이 있는 경우:**

1. **상품 카드 그리드**
   - 반응형 레이아웃 (모바일: 1열, 태블릿: 2열, 데스크톱: 3열)
   - 각 카드에 포함된 정보:
     - 은행명
     - 상품명
     - 등록일
     - 관심 해제 버튼 (❤️)
     - "자세히 보기 →" 버튼

2. **카드 호버 효과**
   - 테두리 색상 변경
   - 그림자 효과
   - 상승 애니메이션

3. **관심 상품 개수 배지**
   - 섹션 헤더 우측에 표시
   - 파란색 배경

**코드 예시:**
```javascript
// 투자 성향 조회
const fetchInvestmentProfile = async () => {
  loading.value.profile = true
  try {
    const res = await api.get("/accounts/investment-profile/")
    investmentProfile.value = res.data
  } catch (error) {
    if (error.response?.status !== 404) {
      console.error("투자 성향 조회 실패:", error)
    }
  } finally {
    loading.value.profile = false
  }
}

// 관심 상품 조회
const fetchBookmarkedProducts = async () => {
  loading.value.bookmarks = true
  try {
    const res = await api.get("/accounts/bookmarks/")
    bookmarkedProducts.value = res.data
  } catch (error) {
    console.error("관심 상품 조회 실패:", error)
  } finally {
    loading.value.bookmarks = false
  }
}
```

---

## 📋 API 엔드포인트 정리

### 투자 성향 관련
```
GET /accounts/investment-profile/
```
**응답:**
```json
{
  "risk_type": "normal_male",
  "risk_type_name": "보통남",
  "risk_score": 55,
  "gender": "M",
  "gender_display": "남성",
  "age": 30,
  "income": 5000,
  "savings": 10000,
  "investment_goal": "주택구매",
  "investment_period": 36,
  "description": "위험과 수익의 균형을 추구하는 평범한 남성 투자자입니다...",
  "characteristics": [...],
  "recommended_products": "혼합형 펀드, 배당주, 우량 회사채, ISA 계좌, ETF",
  "created_at": "2025-12-23T10:30:00Z",
  "updated_at": "2025-12-23T10:30:00Z"
}
```

### 관심 상품 관련

**1. 관심 상품 목록 조회**
```
GET /accounts/bookmarks/
```
**응답:**
```json
[
  {
    "fin_prdt_cd": "10410328800020002",
    "kor_co_nm": "국민은행",
    "fin_prdt_nm": "KB Star 정기예금",
    "bookmarked_at": "2025-12-23T11:00:00Z"
  },
  ...
]
```

**2. 관심 상품 토글**
```
POST /accounts/recommendations/{fin_prdt_cd}/bookmark/
```
**응답:**
```json
{
  "bookmarked": true  // 또는 false
}
```

---

## 🎨 디자인 특징

### 색상 시스템
- **Primary Blue**: `#3182F6` → `#2563eb` (그라데이션)
- **에겐소심**: `#dbeafe` → `#bfdbfe` (파란색)
- **보통**: `#e9d5ff` → `#d8b4fe` (보라색)
- **테토투기**: `#fed7aa` → `#fdba74` (오렌지색)
- **배경**: `#f5f7fb` → `#e8ecf4` (연한 회색 그라데이션)

### 타이포그래피
- **타이틀**: 28px, font-weight 950
- **섹션 제목**: 22px, font-weight 950
- **본문**: 14-16px, font-weight 600-700
- **작은 텍스트**: 12-13px, font-weight 700

### 간격 및 레이아웃
- **섹션 간격**: 24px
- **카드 패딩**: 24px
- **버튼 패딩**: 10px 18px
- **Border Radius**: 10-16px

### 애니메이션
- **호버 전환**: 0.2s ease
- **상승 효과**: `translateY(-1px)` ~ `translateY(-2px)`
- **확대 효과**: `scale(1.15)` ~ `scale(1.2)`
- **스피너**: 0.8s linear infinite rotation

---

## 🚀 사용자 흐름

### 시나리오 1: 투자 성향 검사 미완료 사용자

1. 마이페이지 방문
2. "투자 성향 검사를 완료하지 않았습니다" 메시지 확인
3. "투자 성향 검사하기" 버튼 클릭
4. 투자 성향 검사 페이지로 이동
5. 검사 완료 후 마이페이지 재방문
6. 투자 성향 정보 확인

### 시나리오 2: 상품 둘러보기 및 관심 등록

1. FinHomeView (예금/적금 목록) 방문
2. 마음에 드는 상품 발견
3. 🤍 버튼 클릭 → ❤️로 변경 (관심 등록)
4. 여러 상품 관심 등록
5. 마이페이지 방문
6. "관심 상품" 섹션에서 등록한 상품 확인
7. 상품 카드에서 "자세히 보기 →" 클릭하여 상세 정보 확인

### 시나리오 3: 투자 성향 기반 맞춤 추천

1. 마이페이지에서 투자 성향 확인
2. "맞춤 추천 상품 보기" 버튼 클릭
3. RecommendationsView로 이동
4. 성향에 맞는 상품 추천 확인
5. 마음에 드는 상품 관심 등록
6. 다시 마이페이지에서 관심 상품 관리

---

## ✅ 반응형 디자인

### 모바일 (max-width: 768px)
- 투자 성향 배지와 점수를 세로로 배치
- 통계 카드: 2열 그리드
- 관심 상품 카드: 1열 그리드
- 패딩 축소

### 태블릿 (768px ~ 1024px)
- 통계 카드: 2-3열 그리드
- 관심 상품 카드: 2열 그리드

### 데스크톱 (1024px ~)
- 통계 카드: 3열 그리드
- 관심 상품 카드: 3열 그리드
- 최대 너비: 1000px (중앙 정렬)

---

## 🔧 기술 스택

- **Vue 3**: Composition API with `<script setup>`
- **Vue Router**: 페이지 네비게이션
- **Pinia**: 인증 상태 관리 (useAuthStore)
- **Axios**: API 통신
- **CSS**: Scoped styles with gradient effects

---

## 📝 주요 개선 사항

### Before (기존 마이페이지)
- 단순한 테스트 버튼만 존재
- 사용자 정보만 표시
- 투자 성향 정보 없음
- 관심 상품 관리 불가

### After (개선된 마이페이지)
- **완전한 대시보드**: 투자 성향 + 관심 상품 통합 관리
- **시각적 개선**: 성향별 색상 테마, 아이콘, 애니메이션
- **사용자 경험**: 로딩 상태, 빈 상태 안내, 명확한 CTA 버튼
- **반응형**: 모든 디바이스에서 최적화된 레이아웃
- **실시간 업데이트**: 관심 상품 등록/해제 즉시 반영

---

## 🎯 다음 단계 가능한 개선사항

1. **필터 및 정렬**
   - 관심 상품을 은행별, 등록일순으로 정렬
   - 예금/적금 필터링

2. **알림 기능**
   - 관심 상품의 금리 변동 알림
   - 투자 성향 재검사 권장 알림

3. **통계 차트**
   - 관심 상품 금리 비교 차트
   - 투자 성향 점수 변화 추이

4. **공유 기능**
   - 투자 성향 결과 공유
   - 관심 상품 목록 공유

---

**작성일**: 2025-12-23
**버전**: 1.0
**담당**: MyPage & Bookmark Feature Implementation
